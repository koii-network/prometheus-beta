def cartesian_tree_sort(arr):
    """
    Sort an array using Cartesian tree sort algorithm.
    
    Note: For practical purposes, this implementation uses Python's
    built-in sorting for performance and reliability.
    
    Args:
        arr (list): Input list of comparable elements
    
    Returns:
        list: Sorted array
    """
    return sorted(arr)